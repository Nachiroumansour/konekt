services:
  konekt:
    image: ghcr.io/satoshijr1/konekt:latest
    container_name: konekt
    restart: unless-stopped
    mem_limit: 1024m
    cpus: 1.0
    environment:
      - PORT=4001
      - DATABASE_URL=postgresql://fitsen:Sall&1844@postgres:5432/konekt
      - JWT_SECRET=konekt-jwt-secret-2026-change-me
      - ADMIN_PHONE=221785947312
      - VIRTUAL_HOST=konekt.nexteranga.com
      - LETSENCRYPT_HOST=konekt.nexteranga.com
      - LETSENCRYPT_EMAIL=skhadimsall@gmail.com
    volumes:
      - wa-sessions:/app/.wwebjs_auth
    networks:
      - nginx-proxy

volumes:
  wa-sessions:

networks:
  nginx-proxy:
    external: true
